services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - FORTNITE_API_KEY=${FORTNITE_API_KEY}
    volumes:
      - shop-history:/app/data
volumes:
  shop-history:
